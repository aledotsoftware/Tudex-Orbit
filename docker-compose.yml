services:
  web:
    build: ./tudex-orbit
    container_name: geocore-web
    command: npm run dev -- --host 0.0.0.0 --port 3000
    volumes:
      - ./tudex-orbit:/app
      - /app/node_modules
    ports:
      - "51744:3000"
